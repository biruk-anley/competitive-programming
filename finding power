class Solution:
    def myPow(self, x: float, n: int) -> float:
        if n==0:
            return 1
        elif(x==0):
            return 0
        else:
            return((x*x)*(x**(n-2)))
        
